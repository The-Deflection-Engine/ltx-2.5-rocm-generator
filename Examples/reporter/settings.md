Derived from the original output filename
(`output_1024x576_289f_seed1793758842.mp4` before renaming) and confirmed
against the file itself with `ffprobe` — no accompanying log was saved for
this render, so the guidance-mode fields below are inferred, not measured.

- **Resolution:** 1024x576
- **Frames:** 289 (12.0s at 24fps, confirmed via `ffprobe`)
- **Seed:** 1793758842
- **CFG quality mode / STG:** off — the filename carries no `_cfg`/`_stg` suffix
- **2-stage upscale:** off — no `_up` suffix, and the resolution matches a base render
